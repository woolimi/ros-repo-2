# SC-03: 사용 중인 카트 접속 차단

| 항목 | 내용 |
|---|---|
| **상태 전환** | — (SM 변경 없음) |
| **트리거** | `session_check` 응답 = 다른 사용자가 사용 중 |
| **우선순위** | LOW |
| **연관** | UR-05, SR-12 |

---

## 사전 조건

- 로봇 #54가 `test01` 계정으로 이미 사용 중 (SM = `TRACKING`)
- 다른 기기에서 같은 로봇 #54 QR URL 접속 시도

## 시험 절차

1. **다른 브라우저(시크릿 창)에서** `http://localhost:8501/?robot_id=54` 접속

2. **`session_check` 결과 확인**
   - control_service: `ROBOT.active_user_id` 가 NULL이 아님 → 사용 중 응답 전송

3. **화면 확인**

## 예상 결과

- `blocked.html` 표시: "현재 다른 사용자가 이 카트를 이용 중입니다."
- [새로고침] 버튼 표시
- 로봇 SM 상태 변경 없음
