# SC-71: HALTED → staff_resolved → CHARGING

| 항목 | 내용 |
|---|---|
| **상태 전환** | `HALTED` → `CHARGING` |
| **트리거** | `staff_resolved` |
| **우선순위** | HIGH |
| **연관** | UR-61, UR-53, SR-57, SR-59 |

---

## 사전 조건

- SC-70 완료 후 상태: SM = `HALTED`, 로봇 현장 정지 중
- [관제] 스태프 호출 패널에 HALTED 항목 표시 중

## 시험 절차

1. **직원이 현장으로 이동하여 로봇을 충전소로 수동 이동** (실 환경)
   - 로봇 전원 유지한 채 물리적으로 충전소 슬롯으로 이동

2. **[관제] 스태프 호출 패널에서 Robot#54 HALTED 항목 확인**

3. **[관제] [초기화] 버튼 클릭**
   - 확인 다이얼로그 → 확인
   - admin_ui → control_service: `{"cmd": "staff_resolved", "robot_id": 54}`
   - control_service → Pi: `{"cmd": "staff_resolved"}`

4. **Pi 처리 확인**
   - `sm.trigger('staff_resolved')` → SM = `CHARGING`
   - `terminate_session()` 호출
   - `ROBOT.active_user_id = NULL`

5. **SM = `CHARGING` 전환 확인**
   ```bash
   ros2 topic echo /robot_54/status
   # mode: CHARGING
   ```

6. **LED 변화 확인** — 흰색 점멸 → 빨강(충전 중)

7. **[앱] staff_resolved WebSocket 수신 → login.html 리다이렉트 확인**

8. **[관제] 스태프 호출 항목 "✓ 처리됨" 회색 처리 확인**

## 예상 결과

- SM = `CHARGING`
- `is_locked_return` = False (HALTED는 is_locked_return 무관)
- 세션 종료: `SESSION.is_active=0`, `ROBOT.active_user_id=NULL`
- LED = 빨강 (정상 충전)
- `STAFF_CALL_LOG.resolved_at` 갱신
- EVENT_LOG에 `STAFF_RESOLVED` 기록
