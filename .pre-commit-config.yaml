repos:
  - repo: local
    hooks:
      - id: session-login-gate
        name: Session/Login Regression Gate
        entry: bash scripts/hooks/precommit_session_gate.sh
        language: system
        pass_filenames: false
        stages: [pre-commit]
